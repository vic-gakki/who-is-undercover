server {
    listen       80;
    listen       [::]:80;
    server_name  localhost;

    # 前端静态文件目录
    root   /usr/share/nginx/html;
    index  index.html;

    # 处理前端路由
    location / {
        try_files $uri $uri/ /index.html;
    }
}